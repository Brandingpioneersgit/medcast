---
import BaseLayout from "@/layouts/BaseLayout.astro";
import HeadMeta from "@/components/HeadMeta.astro";
import SiteHeader from "@/components/SiteHeader.astro";
import SiteFooter from "@/components/SiteFooter.astro";
import ExploreGrid from "@/components/ExploreGrid.astro";
import { getTranslator } from "@/lib/messages";
import { isLocale, localizedPath, type Locale } from "@/lib/i18n";
import { breadcrumbJsonLd, localeUrl, faqJsonLd } from "@/lib/seo";
import { Upload, Lock, ChevronRight, Award, ShieldCheck, ScrollText } from "lucide-react";
import { patientHero, patientConsultation } from "@/lib/images";
import HowItWorks from "@/components/content/HowItWorks.astro";
import WhatToExpect from "@/components/content/WhatToExpect.astro";

export const prerender = false;

const rawLocale = Astro.params.locale ?? "en";
if (!isLocale(rawLocale)) return Astro.redirect(`/en/second-opinion`, 308);
const locale = rawLocale as Locale;
const tc = getTranslator(locale, "common");
const tso = getTranslator(locale, "secondOpinionPage");

const faqs = [
  { question: "Is the second opinion really free?", answer: "Yes. A specialist reviews your case at zero cost. The economics: if you later decide to travel, you pay the hospital directly for treatment — no markup from us. If you don't travel, we've spent an hour on your case. That's the whole deal." },
  { question: "How fast do I get the opinion?", answer: "48 working hours from upload, usually. Complex cases (rare cancers, combined procedures, paediatric referrals) can stretch to 72-96 hours — we tell you upfront if yours is in that bucket." },
  { question: "Which reports should I share?", answer: "Most recent imaging (MRI, CT, X-ray — DICOM files preferred over screenshots), all lab results from the last 6 months, biopsy pathology if any, current medications list, and a one-paragraph history. PDFs, JPGs, or WhatsApp all work. If you don't have something, tell us — we'll say whether the opinion can still be useful." },
  { question: "Who actually writes the opinion?", answer: "A board-certified specialist in the relevant field, currently practising at one of our partner hospitals. Their name, hospital, and credentials are on the report — not a ghost-written template." },
  { question: "Is my data safe?", answer: "Files are encrypted in transit and at rest. Only the assigned doctor and your case manager can read them. Everything is purged 30 days after we deliver the opinion. GDPR-aligned, and we'll sign an NDA on request." },
  { question: "Which specialties do you cover?", answer: "Cardiac, oncology, orthopedics, neurology/neurosurgery, organ transplant, fertility, bariatric, paediatric tertiary. If your case doesn't fit our panel — rare dermatology, say — we'll tell you within an hour rather than take up your time." },
  { question: "What if the second opinion disagrees with my current doctor?", answer: "It happens in about 28% of the cases we see — usually when surgery was recommended and our panel suggests a less-invasive option. We send both views to you in writing so you can take them back to your original doctor. We don't put pressure either way." },
];

const url = new URL(Astro.request.url);
const prefillCondition = url.searchParams.get("condition") ?? "";

const path = "/second-opinion";
const canonical = localeUrl(locale, path);

const crumbs = breadcrumbJsonLd([
  { name: tc("home"), url: localeUrl(locale, "/") },
  { name: tso("breadcrumbSecondOpinion"), url: canonical },
]);

Astro.response.headers.set(
  "Cache-Control",
  "public, s-maxage=86400, stale-while-revalidate=604800",
);

const STEPS = [
  { n: "01", t: tso("step01Title"), d: tso("step01Desc") },
  { n: "02", t: tso("step02Title"), d: tso("step02Desc") },
  { n: "03", t: tso("step03Title"), d: tso("step03Desc") },
  { n: "04", t: tso("step04Title"), d: tso("step04Desc") },
];

const PROMISES = [
  { eyebrow: tso("promise01eyebrow"), title: tso("promise01title"), body: tso("promise01body"), icon: Award },
  { eyebrow: tso("promise02eyebrow"), title: tso("promise02title"), body: tso("promise02body"), icon: ShieldCheck },
  { eyebrow: tso("promise03eyebebrow"), title: tso("promise03title"), body: tso("promise03body"), icon: ScrollText },
];
---

<BaseLayout locale={locale}>
  <Fragment slot="head">
    <HeadMeta
      title={tso("seoTitle")}
      description={tso("seoDescription")}
      path={path}
      locale={locale}
      noindex={locale !== "en"}
    />
    <script type="application/ld+json" is:inline set:html={JSON.stringify(crumbs)} />
    <script type="application/ld+json" is:inline set:html={JSON.stringify(faqJsonLd(faqs))} />
  </Fragment>

  <main id="main" class="min-h-dvh bg-bg text-ink">
    <SiteHeader locale={locale} />

    <div style="background: var(--color-paper); border-bottom: 1px solid var(--color-border-soft);">
      <div class="mx-auto max-w-[1440px] px-7 md:px-10 py-4">
        <nav class="mono text-[12px] flex items-center gap-2 flex-wrap" style="color: var(--color-ink-subtle);" aria-label="Breadcrumb">
          <a href={localizedPath(locale, "/")} class="hover:text-ink">{tc("home")}</a>
          <span aria-hidden="true" class="text-[var(--color-border-strong)]">/</span>
          <span style="color: var(--color-ink);">{tso("breadcrumbSecondOpinion")}</span>
        </nav>
      </div>
    </div>

    <section class="pt-12 md:pt-16">
      <div class="mx-auto max-w-[1440px] px-7 md:px-10">
        <p class="mono uppercase" style="font-size: 11px; letter-spacing: 0.14em; color: var(--color-accent);">
          {tso("eyebrow")}
        </p>
        <div class="grid gap-10 lg:grid-cols-[1.5fr_1fr] lg:gap-14 mt-3">
          <div>
            <h1
              class="display display-tight"
              style="font-size: clamp(2.25rem, 5vw, 4.5rem); line-height: 0.98; font-weight: 400; letter-spacing: -0.03em;"
            >
              {tso("h1")}
            </h1>
            <p class="serif mt-5 max-w-[44rem]" style="font-size: 19px; line-height: 1.5; color: var(--color-ink-muted);">
              {tso("lede")}
            </p>

            {/* YMYL disclaimer — second-opinion is a remote written review,
                not in-person care. Sets expectations + protects users facing
                acute symptoms who may misread us as urgent care. */}
            <div
              role="note"
              class="mt-5"
              style="background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-md); padding: 12px 16px; max-width: 44rem;"
            >
              <p class="serif" style="font-size: 13.5px; line-height: 1.55; color: var(--color-ink-muted);">
                A second opinion is a remote written review and <strong>does not replace in-person physician evaluation</strong>. For acute or worsening symptoms, contact a local doctor or emergency services first.
              </p>
            </div>

            <div
              class="mt-8 grid grid-cols-3"
              style="border-top: 1px solid var(--color-ink); border-bottom: 1px solid var(--color-border);"
            >
              {[
                { l: "Recommend against surgery", v: tso("promise01eyebrow") },
                { l: "Senior specialists", v: tso("promise03eyebebrow") },
                { l: "Now and always", v: tso("promise02eyebrow") },
              ].map((s, i) => (
                <div class="px-4 md:px-5 py-5" style={i > 0 ? {borderInlineStart: "1px solid var(--color-border)"} : {}}>
                  <div class="mono uppercase" style="font-size: 9.5px; letter-spacing: 0.1em; color: var(--color-ink-subtle);">
                    {s.l}
                  </div>
                  <div class="display tnum mt-1.5" style="font-size: 28px;">{s.v}</div>
                </div>
              ))}
            </div>

            <div class="mt-8 flex flex-wrap gap-3">
              <a
                href={localizedPath(locale, `/contact?type=second-opinion${prefillCondition ? `&condition=${prefillCondition}` : ""}`)}
                class="inline-flex items-center gap-2 font-semibold"
                style="background: var(--color-ink); color: var(--color-bg); padding: 14px 24px; border-radius: 9999px;"
              >
                <Upload className="h-4 w-4" />
                {tc("uploadReports")}
              </a>
              <a
                href="https://wa.me/918006210000"
                class="inline-flex items-center gap-2 font-semibold"
                style="border: 1px solid var(--color-border); color: var(--color-ink); padding: 13px 24px; border-radius: 9999px;"
              >
                {tc("messageOnWhatsApp")}
                <ChevronRight className="h-4 w-4 mirror-x" />
              </a>
            </div>
          </div>

          <aside class="lg:sticky lg:top-32 lg:self-start">
            <div class="paper" style="padding: 26px 28px;">
              <div class="mono uppercase" style="font-size: 10px; letter-spacing: 0.14em; color: var(--color-ink-subtle);">
                {tc("howItWorks")}
              </div>
              <ol class="mt-5 space-y-5">
                {STEPS.map((s) => (
                  <li class="grid gap-3" style="grid-template-columns: 40px 1fr;">
                    <div class="display tnum" style="font-size: 22px; line-height: 1; color: var(--color-accent); letter-spacing: -0.02em;">
                      {s.n}
                    </div>
                    <div>
                      <div class="serif" style="font-size: 15.5px; line-height: 1.3; color: var(--color-ink);">
                        {s.t}
                      </div>
                      <p class="mt-1.5 serif" style="font-size: 13.5px; line-height: 1.5; color: var(--color-ink-muted);">
                        {s.d}
                      </p>
                    </div>
                  </li>
                ))}
              </ol>
              <div class="mt-6 pt-5 flex items-start gap-2.5" style="border-top: 1px dashed var(--color-border);">
                <Lock className="h-3.5 w-3.5 mt-0.5 shrink-0" style={{color: "var(--color-ink-subtle)"}} />
                <p class="mono uppercase" style="font-size: 10px; letter-spacing: 0.12em; color: var(--color-ink-subtle);">
                  {tc("encryptedNote")}
                </p>
              </div>
            </div>
          </aside>
        </div>
      </div>
    </section>

    {/* Real-people strip — diverse patients we've helped */}
    <section style="padding: 64px 0 0;">
      <div class="mx-auto w-full max-w-[80rem] px-5 md:px-8">
        <div class="grid gap-6" style="grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));">
          {[
            { region: "africa", id: 11, country: "Lagos, Nigeria", scope: "Cardiac second opinion", outcome: "Avoided open surgery — angioplasty instead" },
            { region: "middle_east", id: 12, country: "Riyadh, KSA", scope: "Oncology review", outcome: "Confirmed local plan, saved travel" },
            { region: "cis", id: 13, country: "Almaty, Kazakhstan", scope: "Spine surgery", outcome: "Switched to non-surgical recovery protocol" },
            { region: "south_asia", id: 14, country: "Dhaka, Bangladesh", scope: "Liver transplant eval", outcome: "Matched to senior surgeon at Medanta" },
            { region: "africa", id: 15, country: "Nairobi, Kenya", scope: "Knee replacement", outcome: "Total hip + knee in single trip" },
          ].map((p) => (
            <article class="paper overflow-hidden" style="padding: 0;">
              <div class="relative" style="aspect-ratio: 4/5; overflow: hidden;">
                <img
                  src={patientHero(p.id, p.region as any, 480)}
                  alt={`Patient from ${p.country}`}
                  width="480"
                  height="600"
                  loading="lazy"
                  decoding="async"
                  style="width: 100%; height: 100%; object-fit: cover;"
                />
                <div class="image-veil" />
              </div>
              <div style="padding: 14px 16px;">
                <div class="mono uppercase" style="font-size: 9.5px; letter-spacing: 0.12em; color: var(--color-ink-subtle);">
                  {p.country}
                </div>
                <div class="serif" style="font-size: 14.5px; font-weight: 500; line-height: 1.3; color: var(--color-ink); margin-top: 4px;">
                  {p.scope}
                </div>
                <div style="font-size: 12px; line-height: 1.4; color: var(--color-ink-muted); margin-top: 4px;">
                  → {p.outcome}
                </div>
              </div>
            </article>
          ))}
        </div>
        <p class="mono" style="font-size: 10.5px; color: var(--color-ink-subtle); margin-top: 14px; letter-spacing: 0.06em; text-transform: uppercase;">
          Stock imagery · real outcomes from past 12 months · names withheld for privacy
        </p>
      </div>
    </section>

    <section style="padding: 88px 0; border-top: 1px solid var(--color-border-soft); margin-top: 56px;">
      <div class="mx-auto w-full max-w-[80rem] px-5 md:px-8">
        <div class="mb-8">
          <div class="mono uppercase" style="font-size: 10px; letter-spacing: 0.14em; color: var(--color-ink-subtle);">
            {tc("ourPromise")}
          </div>
          <h2 class="display mt-3" style="font-size: 32px; letter-spacing: -0.02em;">
            {tso("promise01title") ? tso("promise01title").split(" ").slice(0, 2).join(" ") + " " : ""}<span class="italic-display">{tc("inWriting")}</span>
          </h2>
        </div>
        <ul class="grid grid-cols-1 gap-5 md:grid-cols-3">
          {PROMISES.map((p) => {
            const Icon = p.icon;
            return (
              <li class="paper relative" style="padding: 28px;">
                <div
                  class="flex items-center justify-center"
                  style="width: 44px; height: 44px; border-radius: 12px; background: var(--color-accent-mist, var(--color-accent-soft)); color: var(--color-accent);"
                  aria-hidden="true"
                >
                  <Icon strokeWidth={1.6} className="h-[22px] w-[22px]" />
                </div>
                <div class="display tnum mt-5" style="font-size: 40px; line-height: 0.95; color: var(--color-accent); letter-spacing: -0.02em;">
                  {p.eyebrow}
                </div>
                <div class="serif mt-3" style="font-size: 19px; line-height: 1.25; letter-spacing: -0.005em; color: var(--color-ink);">
                  {p.title}
                </div>
                <p class="mt-2 serif" style="font-size: 14.5px; line-height: 1.55; color: var(--color-ink-muted);">
                  {p.body}
                </p>
              </li>
            );
          })}
        </ul>
      </div>
    </section>

    <section style="padding: 64px 0; border-top: 1px solid var(--color-border-soft);">
      <div class="mx-auto w-full max-w-[80rem] px-5 md:px-8">
        <div class="grid gap-10 lg:grid-cols-[0.35fr_1fr]">
          <div>
            <div class="mono uppercase" style="font-size: 10px; letter-spacing: 0.14em; color: var(--color-ink-subtle);">
              {tc("faq")}
            </div>
            <h2 class="display mt-3" style="font-size: 28px; line-height: 1.1; letter-spacing: -0.015em;">
              {tso("promise01title") ? "Frequently asked" : tc("frequentlyAsked")}
            </h2>
          </div>
          <div class="divide-y divide-[var(--color-border-soft)]">
            {faqs.map((f) => (
              <details class="group py-5">
                <summary class="flex cursor-pointer items-start justify-between gap-6 list-none" style="color: var(--color-ink);">
                  <span class="serif" style="font-size: 18px; line-height: 1.4; font-weight: 500;">{f.question}</span>
                  <span class="mt-1 shrink-0 transition-transform group-open:rotate-45" style="color: var(--color-ink-subtle); font-size: 20px; line-height: 1;" aria-hidden="true">+</span>
                </summary>
                <p class="mt-3 serif" style="color: var(--color-ink-muted); font-size: 16px; line-height: 1.6;">{f.answer}</p>
              </details>
            ))}
          </div>
        </div>
      </div>
    </section>

    <section style="background: var(--color-ink); color: var(--color-bg); padding: 72px 0;">
      <div class="mx-auto w-full max-w-[80rem] px-5 md:px-8">
        <div class="grid gap-8 lg:grid-cols-[1.5fr_1fr] items-end">
          <div>
            <div class="mono uppercase" style="font-size: 11px; letter-spacing: 0.16em; color: rgba(246,241,230,0.55);">
              {tc("readyWhenYouAre")}
            </div>
            <h2 class="display mt-3" style="font-size: clamp(2rem, 4vw, 3.25rem); line-height: 1.02; letter-spacing: -0.02em; color: var(--color-bg);">
              {tc("sendReports")}
            </h2>
          </div>
          <div class="flex flex-wrap gap-3 lg:justify-end">
            <a
              href={localizedPath(locale, "/contact?type=second-opinion")}
              class="inline-flex items-center gap-2 font-semibold"
              style="background: var(--color-bg); color: var(--color-ink); padding: 14px 22px; border-radius: 9999px;"
            >
              <Upload className="h-4 w-4" />
              {tc("uploadReports")}
            </a>
            <a
              href={localizedPath(locale, "/contact")}
              class="inline-flex items-center gap-2 font-semibold"
              style="border: 1px solid rgba(246,241,230,0.35); color: var(--color-bg); padding: 13px 22px; border-radius: 9999px;"
            >
              {tc("talkToCoordinator")}
            </a>
          </div>
        </div>
      </div>
    </section>

    <HowItWorks
      heading="How a second opinion actually works"
      italicWord="actually works"
      eyebrow="The process"
      steps={[
        { title: "Upload your records", description: "Imaging on disc (DICOM), pathology reports, recent labs, treatment summary, and the recommendation you're seeking a second opinion on. Most patients use our secure portal; some send DVDs by post." },
        { title: "Case-manager review", description: "Within 24 hours, a senior coordinator confirms the records are complete and identifies the right specialty for review. Missing pieces are requested before routing." },
        { title: "Specialist routing", description: "We route to a specialist with high volume in your specific case type. For tumor cases, the routing is to a tumor-board reviewer. For surgical cases, to a senior surgeon in that sub-specialty." },
        { title: "Multidisciplinary review (where indicated)", description: "Complex oncology, transplant, and complex cardiac cases get multidisciplinary review. Your case is discussed in a structured forum with multiple specialists." },
        { title: "Written opinion delivered", description: "5-10 days from records receipt. The opinion is in writing — agreement with original recommendation or specific alternative with rationale. Includes references to evidence base where relevant." },
        { title: "Discussion + next steps", description: "Video call with the reviewing specialist if you want to discuss. No obligation to proceed with us; the written opinion is yours to take to your home physician." },
      ]}
      outroNote="Free for the patient. Hospitals pay our coordination fee only if you proceed with treatment through us — second opinion costs nothing whether you do or don't."
    />

    <WhatToExpect
      heading="When a second opinion changes the plan"
      italicWord="changes the plan"
      items={[
        { label: "Cancer staging review", description: "Pathology re-review changes diagnosis or grade in 5-10% of cases. Imaging re-read by sub-specialty radiologist sometimes shifts stage." },
        { label: "Surgical vs medical management", description: "For borderline cases (early prostate cancer, stable angina, asymptomatic findings), opinions can differ. Active surveillance vs immediate surgery is a common shift." },
        { label: "Specific operation choice", description: "Repair vs replacement (mitral valve), partial vs total (joint), open vs minimally invasive — different surgeons recommend differently. Second opinion helps clarify." },
        { label: "Treatment intent", description: "Curative vs palliative, single-modality vs multi-modal. Different specialists frame the goal differently. Worth understanding the framing." },
        { label: "Timing", description: "Some recommendations push toward immediate intervention; alternatives may suggest watchful waiting first. The right answer depends on specific case factors." },
      ]}
    />

    <ExploreGrid
      locale={locale}
      eyebrow="If you decide to act on the opinion"
      heading="Where the second opinion can take you next"
      lede="A second opinion isn't the end of the journey. The pages below cover the practical paths if it confirms — or contradicts — your existing plan."
      items={[
        { eyebrow: "Match", label: "Match Me quiz", href: "/match-me", hint: "Concern + budget → recommended path in 60s." },
        { eyebrow: "Find", label: "Find a specialist", href: "/find-specialist", hint: "Specialty + condition → matched surgeon." },
        { eyebrow: "Quote", label: "Get a treatment quote", href: "/quote", hint: "5-minute form — 3 hospital options + cost." },
        { eyebrow: "Compare", label: "Compare doctors", href: "/compare/doctors", hint: "Years, patients treated, languages, fee." },
        { eyebrow: "Compare", label: "Compare hospitals", href: "/compare/hospitals", hint: "Side-by-side rating, accreditations, cost." },
        { eyebrow: "Cost", label: "Cost calculator", href: "/calculator", hint: "Live procedure × country pricing band." },
        { eyebrow: "Process", label: "Editorial + corrections policy", href: "/editorial-policy", hint: "Why we recommend against surgery 28% of the time." },
        { eyebrow: "Reviewers", label: "Medical board", href: "/medical-board", hint: "Reviewers behind specialty pages." },
      ]}
    />

    <SiteFooter locale={locale} />
  </main>
</BaseLayout>