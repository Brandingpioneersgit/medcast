---
import BaseLayout from "@/layouts/BaseLayout.astro";
import HeadMeta from "@/components/HeadMeta.astro";
import SiteHeader from "@/components/SiteHeader.astro";
import SiteFooter from "@/components/SiteFooter.astro";
import ExploreGrid from "@/components/ExploreGrid.astro";
import { isLocale, localizedPath, type Locale } from "@/lib/i18n";
import { breadcrumbJsonLd, localeUrl } from "@/lib/seo";
import { getTranslator } from "@/lib/messages";
import {
  Database,
  Workflow,
  ShieldOff,
  Eye,
  KeyRound,
  Clock,
  Globe2,
  Lock,
  Baby,
  Mail,
  RefreshCw,
  ChevronRight,
} from "lucide-react";

export const prerender = false;

const rawLocale = Astro.params.locale ?? "en";
if (!isLocale(rawLocale)) return Astro.redirect("/en/privacy-policy", 308);
const locale = rawLocale as Locale;
const tc = getTranslator(locale, "common");
const tPv = getTranslator(locale, "privacyPage");

const seoTitle = tPv("seoTitle");
const seoDescription = tPv("seoDescription");
const path = "/privacy-policy";

const crumbs = breadcrumbJsonLd([
  { name: tc("home"), url: localeUrl(locale, "/") },
  { name: tPv("breadcrumbPrivacy"), url: localeUrl(locale, path) },
]);

Astro.response.headers.set(
  "Cache-Control",
  "public, s-maxage=86400, stale-while-revalidate=604800",
);

const LAST_UPDATED = "April 2026";

type Section = {
  id: string;
  icon: any;
  title: string;
  intro?: string;
  bullets?: string[];
  paras?: string[];
  tone?: "default" | "warn" | "accent";
};

const sections: Section[] = [
  {
    id: "what-we-collect",
    icon: Database,
    title: tPv("s1Title"),
    intro: tPv("s1Intro"),
    bullets: [
      tPv("s1B1"),
      tPv("s1B2"),
      tPv("s1B3"),
      tPv("s1B4"),
    ],
  },
  {
    id: "what-we-do",
    icon: Workflow,
    title: tPv("s2Title"),
    bullets: [
      tPv("s2B1"),
      tPv("s2B2"),
      tPv("s2B3"),
      tPv("s2B4"),
    ],
  },
  {
    id: "what-we-never-do",
    icon: ShieldOff,
    title: tPv("s3Title"),
    tone: "warn",
    bullets: [
      tPv("s3B1"),
      tPv("s3B2"),
      tPv("s3B3"),
      tPv("s3B4"),
    ],
  },
  {
    id: "who-sees",
    icon: Eye,
    title: tPv("s4Title"),
    bullets: [
      tPv("s4B1"),
      tPv("s4B2"),
      tPv("s4B3"),
      tPv("s4B4"),
    ],
    paras: [
      tPv("s4P1"),
    ],
  },
  {
    id: "rights",
    icon: KeyRound,
    title: tPv("s5Title"),
    tone: "accent",
    bullets: [
      tPv("s5B1"),
      tPv("s5B2"),
      tPv("s5B3"),
      tPv("s5B4"),
      tPv("s5B5"),
    ],
    paras: [
      tPv("s5P1"),
    ],
  },
  {
    id: "retention",
    icon: Clock,
    title: tPv("s6Title"),
    bullets: [
      tPv("s6B1"),
      tPv("s6B2"),
      tPv("s6B3"),
      tPv("s6B4"),
    ],
  },
  {
    id: "transfers",
    icon: Globe2,
    title: tPv("s7Title"),
    paras: [
      tPv("s7P1"),
    ],
  },
  {
    id: "security",
    icon: Lock,
    title: tPv("s8Title"),
    paras: [
      tPv("s8P1"),
    ],
  },
  {
    id: "children",
    icon: Baby,
    title: tPv("s9Title"),
    paras: [
      tPv("s9P1"),
    ],
  },
  {
    id: "contact",
    icon: Mail,
    title: tPv("s10Title"),
    bullets: [
      tPv("s10B1"),
      tPv("s10B2"),
      tPv("s10B3"),
    ],
  },
  {
    id: "changes",
    icon: RefreshCw,
    title: tPv("s11Title"),
    paras: [
      tPv("s11P1"),
    ],
  },
];

function renderInline(s: string): string {
  return s.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
}
---

<BaseLayout locale={locale}>
  <Fragment slot="head">
    <HeadMeta title={seoTitle} description={seoDescription} path={path} locale={locale} />
    <script type="application/ld+json" is:inline set:html={JSON.stringify(crumbs)} />
  </Fragment>

  <main id="main" class="min-h-dvh" style="background: var(--color-bg); color: var(--color-ink);">
    <SiteHeader locale={locale} />

    <div style="padding: 18px 28px; border-bottom: 1px solid var(--color-border-soft); background: var(--color-paper);">
      <nav class="mono mx-auto max-w-[1440px]" style="font-size: 12px; color: var(--color-ink-subtle);" aria-label="Breadcrumb">
        <a href={localizedPath(locale, "/")} class="hover:text-ink">{tc("home")}</a>
        <span class="mx-1.5">/</span>
        <span style="color: var(--color-ink);">{tPv("breadcrumbPrivacy")}</span>
      </nav>
    </div>

    <section style="padding: 64px 28px 32px;">
      <div class="mx-auto w-full max-w-[1120px]">
        <div class="mono" style="font-size: 11px; letter-spacing: 0.14em; text-transform: uppercase; color: var(--color-accent);">
          {tPv("legal")} · {tPv("lastUpdated")} {LAST_UPDATED}
        </div>
        <h1
          class="display display-tight mt-3"
          style="font-size: clamp(2.25rem, 5vw, 3.5rem); line-height: 1; font-weight: 400; letter-spacing: -0.03em;"
        >
          {tPv("h1Main")} <span class="italic-display">{tPv("h1Italic")}</span>
        </h1>
        <p class="serif mt-5" style="font-size: 19px; line-height: 1.5; color: var(--color-ink-muted); max-width: 720px;">
          {tPv("ledePage")}
        </p>

        <div class="mt-8 grid gap-4 md:grid-cols-3">
          <div class="paper flex items-start gap-3" style="padding: 18px 20px;">
            <span class="shrink-0 mt-0.5" style="color: var(--color-coral, #E85A4F);"><ShieldOff className="h-5 w-5" /></span>
            <div>
              <div class="mono uppercase" style="font-size: 10px; letter-spacing: 0.12em; color: var(--color-ink-subtle);">{tPv("promise01Tag")}</div>
              <div class="serif mt-1" style="font-size: 14.5px; line-height: 1.4; color: var(--color-ink);">{tPv("promise01Text")}</div>
            </div>
          </div>
          <div class="paper flex items-start gap-3" style="padding: 18px 20px;">
            <span class="shrink-0 mt-0.5" style="color: var(--color-accent);"><Lock className="h-5 w-5" /></span>
            <div>
              <div class="mono uppercase" style="font-size: 10px; letter-spacing: 0.12em; color: var(--color-ink-subtle);">{tPv("promise02Tag")}</div>
              <div class="serif mt-1" style="font-size: 14.5px; line-height: 1.4; color: var(--color-ink);">{tPv("promise02Text")}</div>
            </div>
          </div>
          <div class="paper flex items-start gap-3" style="padding: 18px 20px;">
            <span class="shrink-0 mt-0.5" style="color: var(--color-accent);"><KeyRound className="h-5 w-5" /></span>
            <div>
              <div class="mono uppercase" style="font-size: 10px; letter-spacing: 0.12em; color: var(--color-ink-subtle);">{tPv("promise03Tag")}</div>
              <div class="serif mt-1" style="font-size: 14.5px; line-height: 1.4; color: var(--color-ink);">{tPv("promise03Text")}</div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section style="padding: 24px 28px 88px;">
      <div class="mx-auto w-full max-w-[1120px] grid gap-12 lg:grid-cols-[1fr_0.28fr]">
        <div class="min-w-0 space-y-5">
          {sections.map((s, i) => {
            const Icon = s.icon;
            const num = String(i + 1).padStart(2, "0");
            const tone = s.tone ?? "default";
            const accentColor =
              tone === "warn"
                ? "var(--color-coral, #E85A4F)"
                : "var(--color-accent)";
            const accentBg =
              tone === "warn"
                ? "var(--color-coral-soft, rgba(232,90,79,0.10))"
                : "var(--color-accent-mist, var(--color-accent-soft))";
            const cardBg = tone === "accent" ? "var(--color-accent-mist, var(--color-accent-soft))" : "var(--color-paper)";
            return (
              <article
                id={s.id}
                class="paper"
                style={`padding: 32px 36px; scroll-margin-top: 120px; background: ${cardBg};`}
              >
                <div class="flex items-start gap-5">
                  <div
                    class="flex items-center justify-center shrink-0"
                    style={`width: 48px; height: 48px; border-radius: 12px; background: ${accentBg}; color: ${accentColor};`}
                    aria-hidden="true"
                  >
                    <Icon className="h-[22px] w-[22px]" />
                  </div>
                  <div class="min-w-0 flex-1">
                    <div class="mono tnum" style="font-size: 10.5px; letter-spacing: 0.14em; text-transform: uppercase; color: var(--color-ink-subtle);">
                      {tPv("sectionLabel")} {num}
                    </div>
                    <h2
                      class="display mt-1.5"
                      style="font-size: clamp(1.4rem, 2.4vw, 1.75rem); line-height: 1.15; letter-spacing: -0.015em; font-weight: 400;"
                    >
                      {s.title}
                    </h2>
                  </div>
                </div>

                <div class="mt-5 space-y-4 serif" style="font-size: 16px; line-height: 1.65; color: var(--color-ink-muted);">
                  {s.intro && <p>{s.intro}</p>}
                  {s.bullets && (
                    <ul class="space-y-3 mt-2">
                      {s.bullets.map((b) => (
                        <li class="flex gap-3" style="font-size: 15.5px; line-height: 1.55;">
                          <span style={`color: ${accentColor}; flex-shrink: 0; font-weight: 600;`} aria-hidden="true">→</span>
                          <span set:html={renderInline(b)} />
                        </li>
                      ))}
                    </ul>
                  )}
                  {s.paras?.map((p) => <p>{p}</p>)}
                </div>
              </article>
            );
          })}

          <div
            class="mt-6 flex items-start gap-4"
            style="padding: 20px 24px; border: 1px dashed var(--color-border); border-radius: var(--radius-lg);"
          >
            <span class="shrink-0 mt-0.5" style="color: var(--color-accent);"><Mail className="h-5 w-5" /></span>
            <div>
              <div class="mono uppercase" style="font-size: 10px; letter-spacing: 0.14em; color: var(--color-accent);">
                {tPv("privacyQuestionsLabel")}
              </div>
              <p class="mt-2 serif" style="font-size: 15.5px; line-height: 1.5; color: var(--color-ink-muted);" set:html={tPv("privacyQuestionsBody").replace("medcastsdigital@gmail.com", '<a href="mailto:medcastsdigital@gmail.com" style="color: var(--color-ink);" class="underline">medcastsdigital@gmail.com</a>')} />
            </div>
          </div>
        </div>

        <aside class="hidden lg:block">
          <div class="lg:sticky lg:top-28 lg:self-start space-y-6">
            <nav
              aria-label="On this page"
              style="padding: 18px 0; border-top: 1px solid var(--color-ink);"
            >
              <div class="mono uppercase" style="font-size: 9.5px; letter-spacing: 0.14em; color: var(--color-ink-subtle);">
                {tPv("onThisPage")}
              </div>
              <ol class="mt-3 space-y-2 tnum">
                {sections.map((s, i) => (
                  <li class="flex gap-2.5">
                    <span class="mono" style="font-size: 11px; color: var(--color-ink-subtle);">{String(i + 1).padStart(2, "0")}</span>
                    <a
                      href={`#${s.id}`}
                      class="serif hover:text-accent transition-colors"
                      style="font-size: 13.5px; line-height: 1.4; color: var(--color-ink);"
                    >
                      {s.title}
                    </a>
                  </li>
                ))}
              </ol>
            </nav>

            <a
              href={localizedPath(locale, "/terms")}
              class="paper group block transition hover:-translate-y-0.5"
              style="padding: 18px 20px;"
            >
              <div class="mono uppercase" style="font-size: 10px; letter-spacing: 0.14em; color: var(--color-accent);">
                {tPv("termsOfServiceLabel")}
              </div>
              <div class="serif mt-2" style="font-size: 16px; line-height: 1.3; color: var(--color-ink);" set:html={tPv("termsTagline")} />
              <div class="mt-3 mono uppercase inline-flex items-center gap-1 group-hover:text-accent transition-colors" style="font-size: 10.5px; letter-spacing: 0.14em; color: var(--color-ink-muted);">
                {tPv("termsReadLink")}
                <ChevronRight className="h-3 w-3 mirror-x" />
              </div>
            </a>
          </div>
        </aside>
      </div>
    </section>

    <ExploreGrid
      locale={locale}
      eyebrow={tPv("exploreEyebrow")}
      heading={tPv("exploreHeading")}
      lede={tPv("exploreLede")}
      items={[
        { eyebrow: tPv("exploreItem1Eyebrow"), label: tPv("exploreItem1Label"), href: "/terms", hint: tPv("exploreItem1Hint") },
        { eyebrow: tPv("exploreItem2Eyebrow"), label: tPv("exploreItem2Label"), href: "/editorial-policy", hint: tPv("exploreItem2Hint") },
        { eyebrow: tPv("exploreItem3Eyebrow"), label: tPv("exploreItem3Label"), href: "/security", hint: tPv("exploreItem3Hint") },
        { eyebrow: tPv("exploreItem4Eyebrow"), label: tPv("exploreItem4Label"), href: "/incident-report", hint: tPv("exploreItem4Hint") },
        { eyebrow: tPv("exploreItem5Eyebrow"), label: tPv("exploreItem5Label"), href: "/medical-board", hint: tPv("exploreItem5Hint") },
        { eyebrow: tPv("exploreItem6Eyebrow"), label: tPv("exploreItem6Label"), href: "/accreditation", hint: tPv("exploreItem6Hint") },
        { eyebrow: tPv("exploreItem7Eyebrow"), label: tPv("exploreItem7Label"), href: "/about", hint: tPv("exploreItem7Hint") },
        { eyebrow: tPv("exploreItem8Eyebrow"), label: tPv("exploreItem8Label"), href: "/contact", hint: tPv("exploreItem8Hint") },
      ]}
    />

    <SiteFooter locale={locale} />
  </main>
</BaseLayout>
