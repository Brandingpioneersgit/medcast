---
import BaseLayout from "@/layouts/BaseLayout.astro";
import HeadMeta from "@/components/HeadMeta.astro";
import SiteHeader from "@/components/SiteHeader.astro";
import SiteFooter from "@/components/SiteFooter.astro";
import ExploreGrid from "@/components/ExploreGrid.astro";
import { isLocale, localizedPath, type Locale } from "@/lib/i18n";
import { breadcrumbJsonLd, localeUrl } from "@/lib/seo";
import { getTranslator } from "@/lib/messages";
import {
  Building2,
  Briefcase,
  ShieldOff,
  CircleDollarSign,
  CheckCircle2,
  FileCheck,
  Hospital,
  Stethoscope,
  Undo2,
  Scale,
  ShieldCheck,
  Gavel,
  Copyright,
  AlertTriangle,
  RefreshCw,
  Mail,
  ChevronRight,
} from "lucide-react";

export const prerender = false;

const rawLocale = Astro.params.locale ?? "en";
if (!isLocale(rawLocale)) return Astro.redirect("/en/terms", 308);
const locale = rawLocale as Locale;
const tc = getTranslator(locale, "common");
const tTm = getTranslator(locale, "termsPage");

const seoTitle = tTm("seoTitle");
const seoDescription = tTm("seoDescription");
const path = "/terms";

const crumbs = breadcrumbJsonLd([
  { name: tc("home"), url: localeUrl(locale, "/") },
  { name: tTm("breadcrumbTerms"), url: localeUrl(locale, path) },
]);

Astro.response.headers.set(
  "Cache-Control",
  "public, s-maxage=86400, stale-while-revalidate=604800",
);

const LAST_UPDATED = "April 2026";

type Section = {
  id: string;
  icon: any;
  title: string;
  intro?: string;
  bullets?: string[];
  paras?: string[];
  tone?: "default" | "warn" | "accent";
};

const sections: Section[] = [
  {
    id: "who-we-are",
    icon: Building2,
    title: tTm("title1"),
    paras: [tTm("body1")],
  },
  {
    id: "what-we-do",
    icon: Briefcase,
    title: tTm("title2"),
    bullets: [
      tTm("bullets2Item1"),
      tTm("bullets2Item2"),
      tTm("bullets2Item3"),
      tTm("bullets2Item4"),
      tTm("bullets2Item5"),
    ],
  },
  {
    id: "what-we-dont",
    icon: ShieldOff,
    title: tTm("title3"),
    tone: "warn",
    bullets: [
      tTm("bullets3Item1"),
      tTm("bullets3Item2"),
      tTm("bullets3Item3"),
      tTm("bullets3Item4"),
    ],
  },
  {
    id: "how-paid",
    icon: CircleDollarSign,
    title: tTm("title4"),
    tone: "accent",
    paras: [tTm("body4Para1"), tTm("body4Para2")],
  },
  {
    id: "responsibilities",
    icon: CheckCircle2,
    title: tTm("title5"),
    bullets: [
      tTm("bullets5Item1"),
      tTm("bullets5Item2"),
      tTm("bullets5Item3"),
      tTm("bullets5Item4"),
    ],
  },
  {
    id: "quote-validity",
    icon: FileCheck,
    title: tTm("title6"),
    paras: [tTm("body6")],
  },
  {
    id: "hospital-independence",
    icon: Hospital,
    title: tTm("title7"),
    paras: [tTm("body7")],
  },
  {
    id: "second-opinions",
    icon: Stethoscope,
    title: tTm("title8"),
    paras: [tTm("body8")],
  },
  {
    id: "cancellation",
    icon: Undo2,
    title: tTm("title9"),
    paras: [tTm("body9")],
  },
  {
    id: "liability",
    icon: Scale,
    title: tTm("title10"),
    paras: [tTm("body10")],
  },
  {
    id: "indemnification",
    icon: ShieldCheck,
    title: tTm("title11"),
    paras: [tTm("body11")],
  },
  {
    id: "governing-law",
    icon: Gavel,
    title: tTm("title12"),
    paras: [tTm("body12")],
  },
  {
    id: "ip",
    icon: Copyright,
    title: tTm("title13"),
    paras: [tTm("body13")],
  },
  {
    id: "medical-disclaimer",
    icon: AlertTriangle,
    title: tTm("title14"),
    tone: "warn",
    paras: [tTm("body14")],
  },
  {
    id: "changes",
    icon: RefreshCw,
    title: tTm("title15"),
    paras: [tTm("body15")],
  },
  {
    id: "contact",
    icon: Mail,
    title: tTm("title16"),
    bullets: [
      tTm("bullets16Item1"),
      tTm("bullets16Item2"),
      tTm("bullets16Item3"),
    ],
  },
];

// Lightweight markdown for **bold** in bullets
function renderInline(s: string): string {
  return s.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
}
---

<BaseLayout locale={locale}>
  <Fragment slot="head">
    <HeadMeta title={seoTitle} description={seoDescription} path={path} locale={locale} />
    <script type="application/ld+json" is:inline set:html={JSON.stringify(crumbs)} />
  </Fragment>

  <main id="main" class="min-h-dvh" style="background: var(--color-bg); color: var(--color-ink);">
    <SiteHeader locale={locale} />

    <div style="padding: 18px 28px; border-bottom: 1px solid var(--color-border-soft); background: var(--color-paper);">
      <nav class="mono mx-auto max-w-[1440px]" style="font-size: 12px; color: var(--color-ink-subtle);" aria-label="Breadcrumb">
        <a href={localizedPath(locale, "/")} class="hover:text-ink">{tc("home")}</a>
        <span class="mx-1.5">/</span>
        <span style="color: var(--color-ink);">{tTm("breadcrumbTerms")}</span>
      </nav>
    </div>

    <section style="padding: 64px 28px 32px;">
      <div class="mx-auto w-full max-w-[1120px]">
        <div class="mono" style="font-size: 11px; letter-spacing: 0.14em; text-transform: uppercase; color: var(--color-accent);">
          {tTm("legalLastUpdated").replace("{date}", LAST_UPDATED)}
        </div>
        <h1
          class="display display-tight mt-3"
          style="font-size: clamp(2.25rem, 5vw, 3.5rem); line-height: 1; font-weight: 400; letter-spacing: -0.03em;"
        >
          {tTm("h1Prefix")} <span class="italic-display">{tTm("h1Italic")}</span>
        </h1>
        <p class="serif mt-5" style="font-size: 19px; line-height: 1.5; color: var(--color-ink-muted); max-width: 720px;">
          {tTm("lede")}
        </p>
      </div>
    </section>

    <section style="padding: 24px 28px 88px;">
      <div class="mx-auto w-full max-w-[1120px] grid gap-12 lg:grid-cols-[1fr_0.28fr]">
        <div class="min-w-0 space-y-5">
          {sections.map((s, i) => {
            const Icon = s.icon;
            const num = String(i + 1).padStart(2, "0");
            const tone = s.tone ?? "default";
            const accentColor =
              tone === "warn"
                ? "var(--color-coral, #E85A4F)"
                : "var(--color-accent)";
            const accentBg =
              tone === "warn"
                ? "var(--color-coral-soft, rgba(232,90,79,0.10))"
                : "var(--color-accent-mist, var(--color-accent-soft))";
            const cardBg = tone === "accent" ? "var(--color-accent-mist, var(--color-accent-soft))" : "var(--color-paper)";
            return (
              <article
                id={s.id}
                class="paper"
                style={`padding: 32px 36px; scroll-margin-top: 120px; background: ${cardBg};`}
              >
                <div class="flex items-start gap-5">
                  <div
                    class="flex items-center justify-center shrink-0"
                    style={`width: 48px; height: 48px; border-radius: 12px; background: ${accentBg}; color: ${accentColor};`}
                    aria-hidden="true"
                  >
                    <Icon className="h-[22px] w-[22px]" />
                  </div>
                  <div class="min-w-0 flex-1">
                    <div class="mono tnum" style={`font-size: 10.5px; letter-spacing: 0.14em; text-transform: uppercase; color: var(--color-ink-subtle);`}>
                      {tTm("sectionLabel")} {num}
                    </div>
                    <h2
                      class="display mt-1.5"
                      style="font-size: clamp(1.4rem, 2.4vw, 1.75rem); line-height: 1.15; letter-spacing: -0.015em; font-weight: 400;"
                    >
                      {s.title}
                    </h2>
                  </div>
                </div>

                <div class="mt-5 space-y-4 serif" style="font-size: 16px; line-height: 1.65; color: var(--color-ink-muted);">
                  {s.intro && <p>{s.intro}</p>}
                  {s.paras?.map((p) => <p>{p}</p>)}
                  {s.bullets && (
                    <ul class="space-y-3 mt-2">
                      {s.bullets.map((b) => (
                        <li class="flex gap-3" style="font-size: 15.5px; line-height: 1.55;">
                          <span style={`color: ${accentColor}; flex-shrink: 0; font-weight: 600;`} aria-hidden="true">→</span>
                          <span set:html={renderInline(b)} />
                        </li>
                      ))}
                    </ul>
                  )}
                </div>
              </article>
            );
          })}

          <div
            class="mt-6 flex items-start gap-4"
            style="padding: 20px 24px; border: 1px dashed var(--color-border); border-radius: var(--radius-lg);"
          >
            <span class="shrink-0 mt-0.5" style="color: var(--color-accent);"><Mail className="h-5 w-5" /></span>
            <div>
              <div class="mono uppercase" style="font-size: 10px; letter-spacing: 0.14em; color: var(--color-accent);">
                {tTm("questionsHeading")}
              </div>
              <p class="mt-2 serif" style="font-size: 15.5px; line-height: 1.5; color: var(--color-ink-muted);">
                {tTm("questionsBodyPre")}<a href="mailto:medcastsdigital@gmail.com" style="color: var(--color-ink);" class="underline">medcastsdigital@gmail.com</a>{tTm("questionsBodyPost")}
              </p>
            </div>
          </div>
        </div>

        <aside class="hidden lg:block">
          <div class="lg:sticky lg:top-28 lg:self-start space-y-6">
            <nav
              aria-label="On this page"
              style="padding: 18px 0; border-top: 1px solid var(--color-ink);"
            >
              <div class="mono uppercase" style="font-size: 9.5px; letter-spacing: 0.14em; color: var(--color-ink-subtle);">
                {tTm("onThisPage")}
              </div>
              <ol class="mt-3 space-y-2 tnum">
                {sections.map((s, i) => (
                  <li class="flex gap-2.5">
                    <span class="mono" style="font-size: 11px; color: var(--color-ink-subtle);">{String(i + 1).padStart(2, "0")}</span>
                    <a
                      href={`#${s.id}`}
                      class="serif hover:text-accent transition-colors"
                      style="font-size: 13.5px; line-height: 1.4; color: var(--color-ink);"
                    >
                      {s.title}
                    </a>
                  </li>
                ))}
              </ol>
            </nav>

            <a
              href={localizedPath(locale, "/privacy-policy")}
              class="paper group block transition hover:-translate-y-0.5"
              style="padding: 18px 20px;"
            >
              <div class="mono uppercase" style="font-size: 10px; letter-spacing: 0.14em; color: var(--color-accent);">
                {tTm("privacyLabel")}
              </div>
              <div class="serif mt-2" style="font-size: 16px; line-height: 1.3; color: var(--color-ink);">
                {tTm("privacyTeaserPre")}<span class="italic-display">{tTm("privacyTeaserItalic")}</span>
              </div>
              <div class="mt-3 mono uppercase inline-flex items-center gap-1 group-hover:text-accent transition-colors" style="font-size: 10.5px; letter-spacing: 0.14em; color: var(--color-ink-muted);">
                {tTm("readPrivacy")}
                <ChevronRight className="h-3 w-3 mirror-x" />
              </div>
            </a>
          </div>
        </aside>
      </div>
    </section>

    <ExploreGrid
      locale={locale}
      eyebrow={tTm("exploreEyebrow")}
      heading={tTm("exploreHeading")}
      lede={tTm("exploreLede")}
      items={[
        { eyebrow: tTm("exploreItem1Eyebrow"), label: tTm("exploreItem1Label"), href: "/privacy-policy", hint: tTm("exploreItem1Hint") },
        { eyebrow: tTm("exploreItem2Eyebrow"), label: tTm("exploreItem2Label"), href: "/editorial-policy", hint: tTm("exploreItem2Hint") },
        { eyebrow: tTm("exploreItem3Eyebrow"), label: tTm("exploreItem3Label"), href: "/security", hint: tTm("exploreItem3Hint") },
        { eyebrow: tTm("exploreItem4Eyebrow"), label: tTm("exploreItem4Label"), href: "/incident-report", hint: tTm("exploreItem4Hint") },
        { eyebrow: tTm("exploreItem5Eyebrow"), label: tTm("exploreItem5Label"), href: "/medical-board", hint: tTm("exploreItem5Hint") },
        { eyebrow: tTm("exploreItem6Eyebrow"), label: tTm("exploreItem6Label"), href: "/accreditation", hint: tTm("exploreItem6Hint") },
        { eyebrow: tTm("exploreItem7Eyebrow"), label: tTm("exploreItem7Label"), href: "/about", hint: tTm("exploreItem7Hint") },
        { eyebrow: tTm("exploreItem8Eyebrow"), label: tTm("exploreItem8Label"), href: "/contact", hint: tTm("exploreItem8Hint") },
      ]}
    />

    <SiteFooter locale={locale} />
  </main>
</BaseLayout>
